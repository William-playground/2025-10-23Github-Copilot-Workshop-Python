# ポモドーロタイマーWebアプリケーション アーキテクチャ案

## 1. 概要

Flask（Python）＋HTML/CSS/JavaScriptで実装するポモドーロタイマーWebアプリの設計方針をまとめます。

---

## 2. 構成

- **バックエンド**: Flask
  - タイマーや進捗管理ロジックの分離（独立したPythonモジュール・クラス化）
  - APIエンドポイントの提供（進捗データの保存・取得など）
  - データ管理の抽象化（ファイル/DB/メモリの切り替え可能）
  - RESTfulなAPI設計

- **フロントエンド**: HTML/CSS/JavaScript
  - タイマーUI（円グラフ、開始・リセットボタン）
  - 進捗表示（完了数、集中時間）
  - タイマー・進捗計算ロジックの関数化
  - API通信部分の分離

---

## 3. ディレクトリ構成例

```
/プロジェクトルート
│
├── app.py                # Flaskアプリ本体
├── progress_manager.py   # 進捗管理ロジック（分離）
├── templates/
│   └── index.html        # メイン画面
├── static/
│   ├── css/
│   │   └── style.css     # UIスタイル
│   └── js/
│       └── timer.js      # タイマー・進捗ロジック
├── tests/
│   ├── test_app.py       # Flask APIのテスト
│   └── test_timer.js     # JSロジックのテスト
└── README.md
```

---

## 4. ユニットテストのしやすさへの配慮

- ロジックの分離・関数化により、各機能を直接importしてテスト可能
- FlaskのテストクライアントでAPIのリクエスト・レスポンスを検証
- データ管理の抽象化で、テスト時はモックやインメモリ実装に切り替え可能
- JSロジックはJest等で単体テスト可能
- テスト用ディレクトリ `tests/` を設置
- CI/CD連携（GitHub Actions等）で自動テスト実行

---

## 5. 拡張性

- ユーザー認証や履歴管理、分析機能の追加が容易
- モバイル対応やUI/UXの改善も柔軟に対応可能

---

## 6. 実装・運用のポイント

- タイマーは基本的にフロントエンド（JS）で動作
- 進捗データのみサーバー側で保存・管理
- API設計・ロジック分離・テスト容易性を重視

---

以上の方針で設計・実装を進めます。